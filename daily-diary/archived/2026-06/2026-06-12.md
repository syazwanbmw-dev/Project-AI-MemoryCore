# Dev Diary - June 12, 2026
*Session documentation and development record*

---

## 12:50 — mypwa-v2 · OMR Scanner Polish + Merge Production

**Projek:** eNilai (mypwa-v2)
**Fokus:** UI polish OMR Scanner, bug fixes, merge ke production

### Apa yang dibuat

**Template borang (printed form):**
- 2-column layout selalu — `rowsPerCol = ceil(n/2)`, ROW_H dinamik (bukan hardcode /20)
- Huruf A/B/C/D dilukis dalam setiap bubble
- Column centering: L_NUM_X 40→85, R_NUM_X 320→355
- GRID_BOT 770→740 — elak row terakhir tindih dengan anchor penjuru
- ANCHOR_SZ 40→22 — marker lebih kecil
- Tajuk "BORANG JAWAPAN" centered
- Garisan nama panjang hingga hujung (x=350→560)
- Print margin 8mm untuk zoom out

**UI kunci jawapan:**
- Kunci grid pakai `grid-auto-flow:column` — Q1-Q(n/2) kiri, Q(n/2+1)-Qn kanan, match template
- Lazy render: input kosong on load, bubble muncul bila bilangan soalan ditaip
- Label "Masukkan Bilangan Soalan:"

**Mobile & layout:**
- Wrapper `max-width:680px; margin:0 auto` — center pada laptop
- Mobile responsive: button 34px→28px pada ≤600px, `max-width:100%`
- Fix navbar hilang: `<nav id="topbar">` (lama) → `.mobile-header` + `.sidebar-overlay` (standard)
- Semua action buttons pakai `.omr-tab` style (outline navy, konsisten)

**Bug fixes:**
- Duplicate `const grid` SyntaxError dalam `renderKunci()` — punca sidebar kosong + bubble tak keluar
- `detectAnchors`: guard maxDark 25% kuadran — elak false detection bila kamera gelap/ditutup
- Tab "② Scan" panggil `mulaScan()` (bukan `showTab` sahaja) — kamera kini auto-init

### Keputusan penting

- **Jadual guru reset setiap sesi** — `jadual_guru` ada `nama_sesi`, guru perlu daftar semula kelas untuk sesi baru. By design, bukan bug.
- **Password admin** — staging + production masih guna `Admin@1234` (default). Perlu ditukar.

### Merge & Deploy
- Playwright: 9/9 PASS (termasuk omr.spec.js dikemaskini — lazy render, empty default)
- Wrangler dry-run: CLEAN
- Merge test→main: `83919b7` — LIVE production
- CLAUDE.md dikemaskini dengan OMR Scanner section

### Langkah seterusnya
- Tukar password admin (urgent)
- Compact mode Ujian Dalaman (backlog)
