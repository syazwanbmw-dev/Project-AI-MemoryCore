# Dev Diary - June 21, 2026
*Session documentation and development record*

---

## 2026-06-21 (18:58 - Petang) - Reset Password Sub Admin + Security Hardening (sistem-olahraga)

### Ringkasan Sesi
- **Tarikh:** 2026-06-21
- **Masa:** 6:58 PM
- **Jenis Sesi:** Kerja Kod + Security Review
- **Projek:** sistem-olahraga-sekolah

### Topik Utama
- Execute plan Reset Password Sub Admin (superadmin reset password akaun sekolah yang lupa)
- Seed superadmin credential ke db staging `olahraga-test`
- Security review: cari credential hardcoded terdedah di frontend
- Fix default password lemah `123456` + tambah validasi min-6 (frontend & backend)
- Selaraskan dokumentasi (CLAUDE.md + memory)

### Pencapaian & Pembelajaran
- **Feature Reset Password Sub Admin** — 5 task siap, LIVE production (main `2ae8785`):
  - 2 endpoint: `GET /api/superadmin/sekolah/sub-admin` + `PATCH /api/superadmin/admin/reset-password`
  - Guard keselamatan 3 lapis (id_pengguna + id_sekolah + peranan='sub_admin') dalam SELECT & UPDATE
  - UI dalam modal Urus: dropdown sub_admin + password + toggle 👁
  - Pipeline Kata penuh: sight-hone CLEAR → commit-seal SEALED → push → verify
- **Penemuan env architecture (penting):** CI deploy push `test` branch → worker `sistem-olahraga-sekolah-test` (db `olahraga-test`), TAPI BASE_URL ujian + login dragon/f4994 hanya jalan di worker top-level `sistem-olahraga-sekolah` (= production). Dua worker berasingan — ujian selama ni uji production, bukan staging.
- **Seed staging** — `olahraga-test` tiada superadmin (sa_cred=0). Jana hash PBKDF2 untuk f4994 (Node webcrypto, params identik dgn app), INSERT ke `superadmin_credentials`. Login -test berjaya. Suite penuh 18/18 PASS lawan -test.
- **Verifikasi E2E sebenar** — GET→reset→login password baru `success=True`; guard id bukan sub_admin → 404.
- **Security review frontend** — jumpa `superadmin.html:144` ada `value="123456"` (default password lemah, nampak page source). Fix opsyen 1: buang default, wajib superadmin taip sendiri (frontend `required minlength=6` + backend `length<6 → 400`).
- **JWT_SECRET semakan** — DISET betul di production DAN env test (wrangler secret list). Fallback 'sila-tukar-secret-ini' tak pernah dipakai. Selamat.

### Keputusan Penting
- Test `16` (bukan `07`) untuk elak konflik nama; assertion utama = kewujudan elemen (deterministik) bukan kiraan dropdown (flaky ikut data)
- BASE_URL ujian ditukar tuding ke worker `-test` untuk CI hygiene (fail gitignored, lokal sahaja)
- Password test nba3003 & xba3202 distandard ke `1234` (asal hashed, tak boleh pulih) — konvensyen test projek
- Default password fix guna opsyen 1 (buang prefill) bukan auto-jana/force-change — paling selamat & mudah
- Validasi min-6 ditambah di backend daftar sub_admin untuk konsisten dgn endpoint reset (defense-in-depth)

### Momen Berkesan
- Disiplin "sahkan kod sebenar dulu" jimat masa — ID HTML sebenar (`#urus-status-langganan`) berbeza dari andaian plan; kalau ikut plan membuta, JS akan pecah
- Debug deploy "gagal" jadi penemuan architecture: ujian 16 gagal bukan sebab bug, tapi sebab poll worker salah (production vs -test)
- Security review buka mata: isu frontend `123456` nampak semua orang, tapi yang lebih kritikal (JWT_SECRET backend) ternyata dah betul diset

### Langkah Seterusnya
- [ ] (Backlog) Rate limiting pada `/api/login`
- [ ] (Backlog) Code DRY extraction
- [ ] (Optional) Tukar BASE_URL ujian ke -test secara kekal kalau master setuju jadi standard
- [ ] (Optional) Dokumentasi superadmin dalam panduan-pengguna.md kalau master nak mula

---
