# Dev Diary - May 17, 2026
*Session documentation and development record*

---

## 17 Mei 2026 (Petang) - PAJSK Bulk Import — Live Production

### Ringkasan Sesi
- **Tarikh:** 2026-05-17
- **Masa:** Petang
- **Jenis Sesi:** Kerja Kod
- **Projek:** mypwa-v2 (eNilai)

### Topik Utama
- Implement PAJSK Bulk Import dari CSV/Excel
- Backend `POST /api/pajsk/bulk` — D1 batch lookup + UPSERT
- Frontend pajsk.html + admin.html — button Import Pukal, SheetJS parse, result modal, template download

### Pencapaian & Pembelajaran
- Bulk import berjaya siap dan live production
- D1 batch API: 1 round trip lookup + 1 round trip upsert — elak N+1 query
- Normalize: kategori, peringkat, pencapaian case-insensitive → canonical form
- Error per-row dikumpul dan dipapar dalam result modal

### Keputusan Penting
- Guna `LOWER(nama_kelas) + LOWER(nama)` untuk lookup murid — case-insensitive
- UPSERT: ON CONFLICT DO UPDATE — update pencapaian + drive_link (kalau baru tidak kosong)
- nama_kelas dalam lookup guna nilai penuh: "6 DELIMA" bukan "DELIMA"

### Momen Berkesan
- Batch D1 approach berjaya handle import besar tanpa timeout

### Langkah Seterusnya
- [x] Merge ke main — live production

---

## 17 Mei 2026 (Malam) - Auto Drive Folder per Sesi — Live Production

### Ringkasan Sesi
- **Tarikh:** 2026-05-17
- **Masa:** 22:14
- **Jenis Sesi:** Kerja Kod (Subagent-Driven Development)
- **Projek:** mypwa-v2 (eNilai)

### Topik Utama
- Execute plan Auto Drive Folder per Sesi menggunakan Subagent-Driven Development
- Migration 022 — tambah `drive_folder_id TEXT` pada table sesi
- POST /api/sesi — auto-create Drive folder via AppScript selepas INSERT
- pajsk.js — DRY helper `getSesiFolderId()` untuk upload + edit handler
- DELETE /api/sesi — auto-padam folder Drive bila sesi dipadam

### Pencapaian & Pembelajaran
- Pertama kali execute plan penuh dengan Subagent-Driven Development — 5 tasks, 2 reviewers per task
- Subagent review loop tangkap bug: `||` patut `??` dalam fallback chain → fix sebelum merge
- Reviewer code quality Task 3 ada false positive — `JSON.parse` dalam inner try/catch, bukan outer
- `waitUntil` pattern untuk guarantee Drive operation selesai sebelum Worker terminate
- AppScript `deleteFolder` — `getFolderById().setTrashed(true)` untuk soft delete ke Trash

### Keputusan Penting
- Fail silently pattern: AppScript failure tidak block sesi create/delete
- Fallback chain: `(sesi.drive_folder_id ?? env.DRIVE_FOLDER_ID) || null` — null-safe dengan `??`
- `deleteFolder` tambah selepas detect orphan folder gap dalam design — bukan dalam plan asal
- Folder masuk Trash (bukan permanent delete) — boleh recover kalau tersalah padam

### Momen Berkesan
- Subagent reviewer tangkap `??` vs `||` bug — contoh dua tahap review benar-benar berkesan
- DB query staging confirm `drive_folder_id` populated selepas sesi baru ditambah — feature works!
- Flow sesi kini lengkap: tambah sesi → folder auto-cipta, padam sesi → folder ke Trash

### Langkah Seterusnya
- [x] Merge ke main — live production (commit c7a6186)
- [ ] Compact mode Ujian Dalaman (bar 28px, 6 kad belum muat satu halaman)
- [ ] LinkedIn setup + dokumentasi journey

---
